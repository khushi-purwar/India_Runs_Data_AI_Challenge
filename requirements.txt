# Core ranking dependencies (CPU-only, no network at ranking time).
scikit-learn>=1.3,<2.0
numpy>=1.24
scipy>=1.10
PyYAML>=6.0

# Optional: enable the dense semantic engine with `--semantic st`.
# The model must be cached locally before ranking (no network during ranking).
# sentence-transformers>=2.2
